apiVersion: v1
kind: Service
metadata:
  name: {{ include "radarr-chart.fullname" . }}
  labels:
    {{- include "radarr-chart.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "radarr-chart.selectorLabels" . | nindent 4 }}
